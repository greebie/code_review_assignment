from setuptools import setup

setup(
   name='Code Review Assignment',
   version='1.0',
   description='A useful module',
   author='Ryan Deschamps',
   author_email='foomail@foo.com',
   packages=['code_review', 'tests'],  #same as name
)